# Ālo Labs Claude Code Plugin Marketplace (deprecated)

**This repository has moved.** Use the unified marketplace instead:

**https://github.com/alo-labs/agent-plugins**

## Migration

```text
/plugin marketplace add alo-labs/agent-plugins
/plugin install silver-bullet@alo-labs
```

Or from a Silver Bullet checkout:

```bash
bash scripts/install-claude.sh --public-release
```

The marketplace ID remains `alo-labs`; only the source repository changed.
